# npm version commit message — CI skips the heavy native jobs for these commits
# (the release tag triggers release.yaml, which verifies the same commit before publish).
message = "chore(release): %s"
